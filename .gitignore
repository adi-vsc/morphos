# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Output artifacts
out/
runs/
*.npz
*.vtk

# AI-tooling artifacts (never ship on the public engine surface)
.claude/
.claude-flow/
.swarm/
graphify-out/

# OS
.DS_Store
Thumbs.db

# Consolidation areas (kept on disk as the single home, off the public engine surface)
# Internal strategy docs never ship in a public repo.
docs/strategy/
# Absorbed but not-yet-wired code, preserved to mine into backends later.
reference/
# Worked case studies (bio, deprioritized for now).
applications/
# Vendored kernel: keep source + LICENSE tracked, exclude heavy native binaries.
vendor/PicoGK/native/
vendor/**/obj/
vendor/**/bin/
