# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments
.venv/
venv/
env/

# Test / tooling caches
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Output artifacts
out/
output/
checkpoints/
runs/
showcase/
*.npz
*.vtk
*.stl

# AI-tooling artifacts (never ship on the public engine surface)
.claude/
.claude-flow/
.swarm/
graphify-out/

# OS
.DS_Store
Thumbs.db

# Working docs and notes (kept on disk, off the public engine surface).
docs/
# Absorbed but not-yet-wired code, preserved to mine into backends later.
reference/
# Worked case studies (bio, deprioritized for now).
applications/
# Vendored kernel: keep source + LICENSE tracked, exclude heavy native binaries.
vendor/PicoGK/native/
vendor/**/obj/
vendor/**/bin/
